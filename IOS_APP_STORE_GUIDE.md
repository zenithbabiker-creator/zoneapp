# دليل رفع تطبيق "زون للخدمات الزراعية" على متجر آبل App Store 🍎

هذا الدليل الشامل يشرح لك كافة الخطوات البرمجية والإدارية المطلوب اتباعها لرفع التطبيق وتدقيقه على متجر **App Store**.

---

## 1. التجهيزات والمربوطات التي تم إعدادها في المشروع تلقائياً ✅

1. **مشروع iOS الأصلي (`/ios`)**: تم إنشاء بيئة Xcode الكاملة ودعم Capacitor iOS 8.
2. **شروط الخصوصية والأذونات في (`Info.plist`)**:
   - `NSCameraUsageDescription`: استخدام الكاميرا لالتقاط صور المحاصيل والتشخيص الذكي.
   - `NSPhotoLibraryUsageDescription`: اختيار الصور من الألبوم للتحليل.
   - `NSPhotoLibraryAddUsageDescription`: حفظ صور التقارير الزراعية في الجهاز.
   - `NSLocationWhenInUseUsageDescription`: تحديد موقع المزرعة لعرض الطقس والتوصيات.
3. **التجاوب مع شاشات آيفون**:
   - دعم منطقة حماية الشاشة (Safe Area Insets) وشريط السحب السفلي للآيفون.
   - تعطيل التكبير التلقائي عند التركيز على حقول الإدخال.
   - ألوان الهيدر وشريط الحالة مكيّفة مع iOS (`black-translucent`).
4. **معرف التطبيق (Bundle Identifier)**:
   - المعرف المحدد في إعدادات الكاباستور: `com.zoon.agri.app`

---

## 2. متطلبات رفع تطبيق آيفون (Apple Developer Account) 📋

1. **جهاز Mac** مثبت عليه أحدث نسخة من برنامج **Xcode** (مجاني من App Store على الماك).
2. **حساب مطور آبل مفعّل (Apple Developer Program)** (بتكلفة 99$ سنوياً من آبل).

---

## 3. خطوات الرفع الشاملة (خطوة بخطوة) 🚀

### الخطوة الأولى: تجهيز الكود على جهاز الماك
1. قم بتنزيل المشروع أو سحبه على جهاز الماك الخاص بك.
2. افتح المجلد في التيرمنال (Terminal) واكتب:
   ```bash
   npm run ios:build
   ```
3. لفتح المشروع مباشرة في برنامج **Xcode**، نفذ الأمر:
   ```bash
   npm run ios:open
   ```
   *(أو قم بفتح المجلد `ios/App/App.xcworkspace` من خلال Xcode).*

---

### الخطوة الثانية: ضبط التوقيع ورقم البناء (Signing & Capabilities)
1. في Xcode، اضغط على الجذر الرئيسي للبرنامج **App** من القائمة اليسرى.
2. اختر التبويب **Signing & Capabilities**.
3. ضع صح على **Automatically manage signing**.
4. اختر حساب المطور الخاص بك في **Team**.
5. تأكد من أن **Bundle Identifier** هو `com.zoon.agri.app` (أو المعرف الذي اعتمدته في حسابك لدى آبل).
6. تأكد من تحديد **Version** (مثلاً `1.0.0`) و **Build** (مثلاً `1`).

---

### الخطوة الثالثة: إضافة آيقونات التطبيق (App Icons)
1. في Xcode من القائمة اليسرى، اختر مجلد `App` ثم `Assets`.
2. اضغط على `AppIcon`.
3. قم بسحب وإفلات آيقونة التطبيق بمقاسات آبل (يمكنك استخدام أدوات مجانية مثل *appicon.co* لتوليد كافة أحجام آيقونات iOS بنقرة واحدة).

---

### الخطوة الرابعة: إنشاء التطبيق على App Store Connect
1. ادخل إلى موقع [App Store Connect](https://appstoreconnect.apple.com).
2. اختر **Apps** ثم اضغط على زر الإضافة **(+) -> New App**.
3. قم بتعبئة البيانات:
   - **Platforms**: iOS
   - **Name**: زون للخدمات الزراعية
   - **Primary Language**: Arabic (العربية)
   - **Bundle ID**: اختر `com.zoon.agri.app`
   - **SKU**: ضع رمزاً فريداً مثل `zone-agri-ios-01`
   - **User Access**: Full Access

---

### الخطوة الخامسة: عمل الأرشيف والرفع (Archive & Upload)
1. في Xcode، اختر الهدف العلوي لجهاز الوجهة ليكون **Any iOS Device (arm64)** (وليس محاكي Simulator).
2. من القائمة العلوية لـ Xcode اختر:
   **Product** ⬅️ **Archive**.
3. بعد اكتمال بناء الأرشيف، تظهر لك نافذة **Organizer**.
4. اضغط على **Distribute App** ثم اختر **App Store Connect** واضغط **Upload**.
5. اتبع الخطوات واضغط **Upload**. ستقوم آبل بفحص الملف ورفعه إلى App Store Connect خلال بضع دقائق.

---

### الخطوة السادسة: مراجعة وتأكيد النشر (Submit for Review)
1. ادخل مجدداً إلى **App Store Connect** وافتح تطبيقك.
2. اذهب إلى تبويب **TestFlight** للتأكد من وصول البناء واستقراره.
3. اذهب إلى صفحة الإصدار **1.0.0 Prepare for Submission**:
   - أرفق صور الشاشة (Screenshots) لهواتف آيفون.
   - اكتب الوصف، الكلمات المفتاحية (Keywords)، ورابط سياسة الخصوصية.
   - اختر البناء المرفوع (Build).
   - قم بتعبئة استبيان الخصوصية والتصنيف العمري.
4. اضغط على زر **Submit for Review**.
5. تستغرق مراجعة آبل عادة بين **24 إلى 48 ساعة** ويكون تطبيقك بعدها متاحاً للجميع على متجر App Store! 🎉
